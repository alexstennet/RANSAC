# Ground Segmentation
The goal is to segment the drivable road from a point cloud. Once done so with moderate success, fix results and train a neural net to then assist in the labeling of data in a point cloud.
